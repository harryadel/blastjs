{
  "name": "blaze-tools",
  "description": "Compile-time tools for Blaze",
  "main": "./dist/preamble.js",
  "version": "0.1.0",
  "scripts": {
    "test": "jest --runInBand --detectOpenHandles --forceExit __tests__/*",
    "clean": "rimraf lib",
    "build": "cross-env BABEL_ENV=production babel src --out-dir lib",
    "prepublish": "npm run clean && npm run test && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "html-tools": "^0.1.1",
    "htmljs": "^0.1.0",
    "reactive-dict": "^1.0.9",
    "standalone-mongo-id": "^0.1.2",
    "standalone-tracker": "^1.0.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.13.14",
    "@babel/core": "^7.13.15",
    "@babel/preset-env": "^7.13.15",
    "cross-env": "^7.0.3",
    "jest": "^26.6.3",
    "rimraf": "^3.0.2"
  } 
}
