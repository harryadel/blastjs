{
  "description": "Observe changes to various sequence types such as arrays, cursors and objects",
  "version": "0.1.0",
  "name": "observe-sequence",
  "main": "dist/observe-sequence",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "watch": "watch 'npm run build' src",
    "clean": "rimraf ./dist",
    "build": "cross-env BABEL_ENV=production babel src --out-dir ./dist",
    "prepublish": "npm run clean && npm run build"
  },
  "dependencies": {
    "standalone-tracker": "1.0.1",
    "standalone-mongo-id": "0.1.2",
    "diff-sequence": "0.1.0",
    "@reactioncommerce/random": "^1.0.2"
  },
  "devDependencies": {
    "@babel/cli": "^7.13.14",
    "@babel/core": "^7.13.15",
    "@babel/preset-env": "^7.13.15",
    "cross-env": "^7.0.3",
    "jest": "^26.6.3",
    "rimraf": "^3.0.2"
  }
}